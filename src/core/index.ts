import sizes, {
  isTablet,
  screenHeight,
  screenWidth,
  sizesTablet,
} from './size/sizes';
import {colors} from './style/colors';
import {fonts} from './style/fonts';
import {Style} from './style/styles';
export {default as fontsizes} from './style/fontsizes';

export {
  Style,
  sizes,
  screenHeight,
  screenWidth,
  sizesTablet,
  isTablet,
  colors,
  fonts,
};
